defmodule Repo.Migrations.CreateLocalCIndex do
  use Ecto.Migration

  def change do
    create table(:local_c_index) do
      add :op, :string
      add :issuer, :string
      add :receiver, :string
      add :created_on, :string
      add :written_on, :string
      add :sig, :string
      add :expires_on, :string
      add :expired_on, :string
      add :chainable_on, :string
      add :replayable_on, :string
      
    end
    create unique_index(:local_c_index, [:issuer])
  end
end
