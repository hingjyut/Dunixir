defmodule Repo.Migrations.CreateMIndex do
  use Ecto.Migration

  def change do
    create table(:m_index) do
      add :op, :string
      add :pub, :string
      add :created_on, :string
      add :written_on, :string
      add :expires_on, :string
      add :expired_on, :string
      add :revokes_on, :string
      add :revoked_on, :string
      add :leaving, :boolean
    end
    create unique_index(:i_index, [:pub]) #faux car c'est seulement pour le local
  end
end
